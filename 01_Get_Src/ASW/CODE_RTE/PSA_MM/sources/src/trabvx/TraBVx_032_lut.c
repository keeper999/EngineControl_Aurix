/*******************************************************************************
*                           P. C. A.
*
*                    Peugeot Citroen Automobile
*
*        This file is the property of PCA. All rights are reserved
*        by PCA and this file must not be copied or disclosed
*       (in whole or in part) without prior written consent of PCA.
*
********************************************************************************
*
* %name: TraBVx_032_lut.c %
*
* %version: 13.0.build3 %
*
* %date_modified: Wed Feb  8 10:55:10 2012 %
*
*
* %derived_by: e360852 %
* %release: TqStruct/13.0 %
* %full_filespec: TraBVx_032_lut.c-13.0.build3:csrc:2 %
*
*******************************************************************************/
/**************************************************************************************************\
 *** 
 *** Simulink model       : RE_032
 *** TargetLink subsystem : RE_032/F00_Regulation_Regime_BV
 *** Codefile             : trabvx_032_lut.c
 ***
 *** Generated by TargetLink, the dSPACE production quality code generator
 *** Generation date: 2012-01-19 16:22:50
 ***
 *** CODE GENERATOR OPTIONS:
 *** Compiler                            : <unknown>
 *** Target                              : Generic
 *** ANSI-C compatible code              : yes
 *** Optimization level                  : 2
 *** Constant style                      : decimal
 *** Clean code option                   : enabled
 *** Logging mode                        : Do not log anything
 *** Linker sections                     : enabled
 *** Assembler statements                : disabled
 *** Variable name length                : 31 chars
 *** Use global bitfields                : disabled
 *** Stateflow: use of bitfields         : enabled
 *** State activity encoding limit       : 5
 *** Omit zero inits in restart function : disabled
 *** Share fcns between TL subsystems    : enabled
 *** Generate 64bit functions            : enabled
 *** Inlining Threshold                  : 6
 *** Line break limit                    : 100
 *** Target optimized boolean data type  : enabled
 *** Keep saturation elements            : disabled
 *** Extended variable sharing           : disabled
 *** Style definition file               : C:\dSPACE\Matlab\Tl\config\codegen\cconfig.xml
 *** Root style sheet                    : C:\dSPACE\Matlab\Tl\XML\CodeGen\Stylesheets\TL_CSourceCod
 ***                                       eSS.xsl
 *** Enable Multirate codegeneration     : disabled
 *** Add model checksum                  : disabled
 ***
 *** SUBSYS                   CORRESPONDING SIMULINK SUBSYSTEM
 *** S0321                    RE_032/F00_Regulation_Regime_BV
 *** S0322                    F00_Regulation_Regime_BV/032
 *** S0323                    F00_Regulation_Regime_BV/032/F01_01460_10_01243
 *** S0324                    F00_Regulation_Regime_BV/032/Subsystem
 *** S0325                    F00_Regulation_Regime_BV/032/F01_01460_10_01243/F03_Couple_Initialisat
 ***                          ion
 *** S0326                    F00_Regulation_Regime_BV/032/F01_01460_10_01243/F05_Reg_regime_air
 *** S0327                    F00_Regulation_Regime_BV/032/F01_01460_10_01243/F03_Couple_Initialisat
 ***                          ion/rising_edge
 *** S0328                    F00_Regulation_Regime_BV/032/F01_01460_10_01243/F05_Reg_regime_air/F05
 ***                          _Regulation_Regime_Air
 *** S0329                    F00_Regulation_Regime_BV/032/F01_01460_10_01243/F05_Reg_regime_air/F05
 ***                          _Regulation_Regime_Air/F05_01_Filtrage_Couple_Regulation
 *** S03210                   F00_Regulation_Regime_BV/032/F01_01460_10_01243/F05_Reg_regime_air/F05
 ***                          _Regulation_Regime_Air/F05_02_Calcul_Couple_Regulation_Air
 *** S03211                   F00_Regulation_Regime_BV/032/F01_01460_10_01243/F05_Reg_regime_air/F05
 *** _Regulation_Regime_Air/F05_01_Filtrage_Couple_Regulation/DLowPassFilte
 ***                          r_TypeK
 *** S03212                   F00_Regulation_Regime_BV/032/F01_01460_10_01243/F05_Reg_regime_air/F05
 *** _Regulation_Regime_Air/F05_01_Filtrage_Couple_Regulation/DLowPassFilte
 ***                          r_TypeK/DLowPass_K_Part
 *** S03213                   F00_Regulation_Regime_BV/032/F01_01460_10_01243/F05_Reg_regime_air/F05
 *** _Regulation_Regime_Air/F05_01_Filtrage_Couple_Regulation/DLowPassFilte
 ***                          r_TypeK/DetectSat1
 *** S03214                   F00_Regulation_Regime_BV/032/F01_01460_10_01243/F05_Reg_regime_air/F05
 *** _Regulation_Regime_Air/F05_01_Filtrage_Couple_Regulation/DLowPassFilte
 ***                          r_TypeK/IniCdn
 *** S03215                   F00_Regulation_Regime_BV/032/F01_01460_10_01243/F05_Reg_regime_air/F05
 *** _Regulation_Regime_Air/F05_01_Filtrage_Couple_Regulation/DLowPassFilte
 ***                          r_TypeK/UnitDly_ExtIni
 *** S03216                   F00_Regulation_Regime_BV/032/Subsystem/01247
 *** S03217                   F00_Regulation_Regime_BV/032/Subsystem/01248
 *** S03218                   F00_Regulation_Regime_BV/032/Subsystem/01247/F01_01460_10_01247
 *** S03219                   F00_Regulation_Regime_BV/032/Subsystem/01248/F01_01460_10_01248
 *** S03220                   F00_Regulation_Regime_BV/032/Subsystem/01248/F01_01460_10_01248/F02_Li
 ***                          mitation_couple
 *** S03221                   F00_Regulation_Regime_BV/032/Subsystem/01248/F01_01460_10_01248/F03_Ac
 ***                          tivation
 *** S03222                   F00_Regulation_Regime_BV/032/Subsystem/01248/F01_01460_10_01248/F04_Co
 ***                          ordination_Limitation_Couple
 *** S03223                   F00_Regulation_Regime_BV/032/Subsystem/01248/F01_01460_10_01248/F03_Ac
 ***                          tivation/BasculeRS
 *** S03224                   F00_Regulation_Regime_BV/032/Subsystem/01248/F01_01460_10_01248/F03_Ac
 ***                          tivation/falling_edge
 *** S03225                   F00_Regulation_Regime_BV/032/Subsystem/01248/F01_01460_10_01248/F04_Co
 ***                          ordination_Limitation_Couple/F04_01_Calcul_Couple_Desactivation
 *** S03226                   F00_Regulation_Regime_BV/032/Subsystem/01248/F01_01460_10_01248/F04_Co
 ***                          ordination_Limitation_Couple/F04_02_Transition_Couple
 *** S03227                   F00_Regulation_Regime_BV/032/Subsystem/01248/F01_01460_10_01248/F04_Co
 ***                          ordination_Limitation_Couple/F04_02_Transition_Couple/falling_edge
 *** 
 *** SF-NODE   CORRESPONDING STATEFLOW NODE                           DESCRIPTION
 *** 
 *** TargetLink version      : 3.0.1 from 26-May-2009
 *** Code generator version  : Build Id 3.0.1.7 from 2009-May-06 15:28:18
 *** Copyright (c) 2006 dSPACE GmbH
\**************************************************************************************************/

#ifndef _TRABVX_032_LUT_C_
#define _TRABVX_032_LUT_C_

/*----------------------------------------------------------------------------*\
  DEFINES (OPT)
\*----------------------------------------------------------------------------*/
/*----------------------------------------------------------------------------*\
  INCLUDES
\*----------------------------------------------------------------------------*/
#include "TraBVx_032_lut.h"
/*----------------------------------------------------------------------------*\
  DEFINES
\*----------------------------------------------------------------------------*/
/*----------------------------------------------------------------------------*\
  TYPEDEFS
\*----------------------------------------------------------------------------*/
/*----------------------------------------------------------------------------*\
  ENUMS
\*----------------------------------------------------------------------------*/
/*----------------------------------------------------------------------------*\
  VARIABLES
\*----------------------------------------------------------------------------*/
/*----------------------------------------------------------------------------*\
  PARAMETERIZED MACROS
\*----------------------------------------------------------------------------*/
/*----------------------------------------------------------------------------*\
  FUNCTION PROTOTYPES
\*----------------------------------------------------------------------------*/
/*----------------------------------------------------------------------------*\
  INLINE FUNCTIONS
\*----------------------------------------------------------------------------*/
/*----------------------------------------------------------------------------*\
  FUNCTION DEFINITIONS
\*----------------------------------------------------------------------------*/


/**************************************************************************************************\
 ***  FUNCTION:
 ***      TraBVx_032_Tab1DS2I2T2080
 *** 
 ***  DESCRIPTION:
 ***      Generated function for look-up tables.
 *** 
 ***  PARAMETERS:
 ***      Type               Name                Description
 ***      ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 ***      const MAP_TraBVx_032_Tab1DS2I2T2080 *map                 data structure
 ***      UInt8              x                   table input
 ***      UInt8 *            local_xLow          table index found in previous search
 ***
 ***  RETURNS:
 ***      SInt16
 ***
 ***  SETTINGS:
 ***      Search algorithm...................: local search
 ***      Look-up method.....................: Interpolation - Use end values
 ***      Distances fit into given bitlengths: no
 ***      Behavior at boundaries.............: no
 ***      Starting point.....................: - (non-equidistant implementation)
 ***      Scaling unit.......................: - (non-equidistant implementation)
 ***
\**************************************************************************************************/

#define TRABVX_START_SEC_CODE
#include "TraBVx_MemMap.h"
SInt16 TraBVx_032_Tab1DS2I2T2080(const MAP_TraBVx_032_Tab1DS2I2T2080 * map, UInt8 x, UInt8 *
  local_xLow)
{
   /* SLLocal: Default storage class for local variables | Width: 16 */
   SInt16 Aux_I16;
   SInt16 Aux_I16_a;

   /* SLLocal: Default storage class for local variables | Width: 8 */
   UInt8 Aux_U8;
   UInt8 Aux_U8_a;

   /* SLLutLocalConst: Default storage class for local variables | Width: 32 */
   const UInt8 * x_table /* Scaling may differ through function reuse. */;
   const SInt16 * z_table /* Scaling may differ through function reuse. */;

   x_table = map->x_table;
   z_table = map->z_table;
   if (x <= *(x_table)) {
      *(local_xLow) = 0;

      /* Saturation. */
      return z_table[0];
   }
   if (x >= x_table[(UInt8) (map->Nx - 1)]) {
      *(local_xLow) = (UInt8) (map->Nx - 2);
      return z_table[(UInt8) (map->Nx - 1)];
   }
   Aux_U8 = *(local_xLow);
   x_table += Aux_U8;

   /* Local search. */
   if (x < *(x_table)) {
      /* Linear search, start high. */
      do       {
         --(Aux_U8);
         --(x_table);
      }
      while (x < *(x_table));
   }
   else {
      /* Linear search, start low. */
      (x_table)++;
      while (x > *((x_table)++))       {
         (Aux_U8)++;
      }
      x_table -= 2 /* 2. */;
   }
   *(local_xLow) = Aux_U8;
   z_table += Aux_U8;
   Aux_I16 = *((z_table)++);
   Aux_I16_a = *(z_table);

   /* Interpolation. */
   Aux_U8 = (UInt8) (x - x_table[0]);
   Aux_U8_a = (UInt8) (x_table[1] - x_table[0]);
   if (Aux_I16 <= Aux_I16_a) {
      /* Positive slope. */
      Aux_I16 += ((SInt16) (UInt16) ((((UInt32) (UInt16) (((UInt16) Aux_I16_a) - ((UInt16)
       Aux_I16))) * ((UInt32) Aux_U8)) / Aux_U8_a));
   }
   else {
      /* Negative slope. */
      Aux_I16 -= ((SInt16) (UInt16) ((((UInt32) (UInt16) (((UInt16) Aux_I16) - ((UInt16)
       Aux_I16_a))) * ((UInt32) Aux_U8)) / Aux_U8_a));
   }
   return Aux_I16;
}

#define TRABVX_STOP_SEC_CODE
#include "TraBVx_MemMap.h"

/**************************************************************************************************\
 ***  FUNCTION:
 ***      TraBVx_032_Tab1DS2I2T2083
 *** 
 ***  DESCRIPTION:
 ***      Generated function for look-up tables.
 *** 
 ***  PARAMETERS:
 ***      Type               Name                Description
 ***      ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 ***      const MAP_TraBVx_032_Tab1DS2I2T2083 *map                 data structure
 ***      SInt8              x                   table input
 ***      UInt8 *            local_xLow          table index found in previous search
 ***
 ***  RETURNS:
 ***      SInt16
 ***
 ***  SETTINGS:
 ***      Search algorithm...................: local search
 ***      Look-up method.....................: Interpolation - Use end values
 ***      Distances fit into given bitlengths: no
 ***      Behavior at boundaries.............: no
 ***      Starting point.....................: - (non-equidistant implementation)
 ***      Scaling unit.......................: - (non-equidistant implementation)
 ***
\**************************************************************************************************/

#define TRABVX_START_SEC_CODE
#include "TraBVx_MemMap.h"
SInt16 TraBVx_032_Tab1DS2I2T2083(const MAP_TraBVx_032_Tab1DS2I2T2083 * map, SInt8 x, UInt8 *
  local_xLow)
{
   /* SLLocal: Default storage class for local variables | Width: 16 */
   SInt16 Aux_I16;
   SInt16 Aux_I16_a;

   /* SLLocal: Default storage class for local variables | Width: 8 */
   UInt8 Aux_U8;
   UInt8 Aux_U8_a;

   /* SLLutLocalConst: Default storage class for local variables | Width: 32 */
   const SInt8 * x_table /* Scaling may differ through function reuse. */;
   const SInt16 * z_table /* Scaling may differ through function reuse. */;

   x_table = map->x_table;
   z_table = map->z_table;
   if (x <= *(x_table)) {
      *(local_xLow) = 0;

      /* Saturation. */
      return z_table[0];
   }
   if (x >= x_table[(UInt8) (map->Nx - 1)]) {
      *(local_xLow) = (UInt8) (map->Nx - 2);
      return z_table[(UInt8) (map->Nx - 1)];
   }
   Aux_U8 = *(local_xLow);
   x_table += Aux_U8;

   /* Local search. */
   if (x < *(x_table)) {
      /* Linear search, start high. */
      do       {
         --(Aux_U8);
         --(x_table);
      }
      while (x < *(x_table));
   }
   else {
      /* Linear search, start low. */
      (x_table)++;
      while (x > *((x_table)++))       {
         (Aux_U8)++;
      }
      x_table -= 2 /* 2. */;
   }
   *(local_xLow) = Aux_U8;
   z_table += Aux_U8;
   Aux_I16 = *((z_table)++);
   Aux_I16_a = *(z_table);

   /* Interpolation. */
   Aux_U8 = (UInt8) (((UInt8) x) - ((UInt8) x_table[0]));
   Aux_U8_a = (UInt8) (((UInt8) x_table[1]) - ((UInt8) x_table[0]));
   if (Aux_I16 <= Aux_I16_a) {
      /* Positive slope. */
      Aux_I16 += ((SInt16) (UInt16) ((((UInt32) (UInt16) (((UInt16) Aux_I16_a) - ((UInt16)
       Aux_I16))) * ((UInt32) Aux_U8)) / Aux_U8_a));
   }
   else {
      /* Negative slope. */
      Aux_I16 -= ((SInt16) (UInt16) ((((UInt32) (UInt16) (((UInt16) Aux_I16) - ((UInt16)
       Aux_I16_a))) * ((UInt32) Aux_U8)) / Aux_U8_a));
   }
   return Aux_I16;
}

#define TRABVX_STOP_SEC_CODE
#include "TraBVx_MemMap.h"

/**************************************************************************************************\
 ***  FUNCTION:
 ***      TraBVx_032_Tab1DS2I2T523
 *** 
 ***  DESCRIPTION:
 ***      Generated function for look-up tables.
 *** 
 ***  PARAMETERS:
 ***      Type               Name                Description
 ***      ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 ***      const MAP_TraBVx_032_Tab1DS2I2T523 *map                 data structure
 ***      SInt8              x                   table input
 ***      UInt8 *            local_xLow          table index found in previous search
 ***
 ***  RETURNS:
 ***      UInt16
 ***
 ***  SETTINGS:
 ***      Search algorithm...................: local search
 ***      Look-up method.....................: Interpolation - Use end values
 ***      Distances fit into given bitlengths: no
 ***      Behavior at boundaries.............: no
 ***      Starting point.....................: - (non-equidistant implementation)
 ***      Scaling unit.......................: - (non-equidistant implementation)
 ***
\**************************************************************************************************/

#define TRABVX_START_SEC_CODE
#include "TraBVx_MemMap.h"
UInt16 TraBVx_032_Tab1DS2I2T523(const MAP_TraBVx_032_Tab1DS2I2T523 * map, SInt8 x, UInt8 *
  local_xLow)
{
   /* SLLocal: Default storage class for local variables | Width: 16 */
   UInt16 Aux_U16;
   UInt16 Aux_U16_a;

   /* SLLocal: Default storage class for local variables | Width: 8 */
   UInt8 Aux_U8;
   UInt8 Aux_U8_a;

   /* SLLutLocalConst: Default storage class for local variables | Width: 32 */
   const SInt8 * x_table /* Scaling may differ through function reuse. */;
   const UInt16 * z_table /* Scaling may differ through function reuse. */;

   x_table = map->x_table;
   z_table = map->z_table;
   if (x <= *(x_table)) {
      *(local_xLow) = 0;

      /* Saturation. */
      return z_table[0];
   }
   if (x >= x_table[(UInt8) (map->Nx - 1)]) {
      *(local_xLow) = (UInt8) (map->Nx - 2);
      return z_table[(UInt8) (map->Nx - 1)];
   }
   Aux_U8 = *(local_xLow);
   x_table += Aux_U8;

   /* Local search. */
   if (x < *(x_table)) {
      /* Linear search, start high. */
      do       {
         --(Aux_U8);
         --(x_table);
      }
      while (x < *(x_table));
   }
   else {
      /* Linear search, start low. */
      (x_table)++;
      while (x > *((x_table)++))       {
         (Aux_U8)++;
      }
      x_table -= 2 /* 2. */;
   }
   *(local_xLow) = Aux_U8;
   z_table += Aux_U8;
   Aux_U16 = *((z_table)++);
   Aux_U16_a = *(z_table);

   /* Interpolation. */
   Aux_U8 = (UInt8) (((UInt8) x) - ((UInt8) x_table[0]));
   Aux_U8_a = (UInt8) (((UInt8) x_table[1]) - ((UInt8) x_table[0]));
   if (Aux_U16 <= Aux_U16_a) {
      /* Positive slope. */
      Aux_U16 += ((UInt16) ((((UInt32) (UInt16) (Aux_U16_a - Aux_U16)) * ((UInt32) Aux_U8)) / 
       Aux_U8_a));
   }
   else {
      /* Negative slope. */
      Aux_U16 -= ((UInt16) ((((UInt32) (UInt16) (Aux_U16 - Aux_U16_a)) * ((UInt32) Aux_U8)) / 
       Aux_U8_a));
   }
   return Aux_U16;
}

#define TRABVX_STOP_SEC_CODE
#include "TraBVx_MemMap.h"

/**************************************************************************************************\
 ***  FUNCTION:
 ***      TraBVx_032_Tab2DS34I2T16651
 *** 
 ***  DESCRIPTION:
 ***      Generated function for 2D look-up tables with common X- and Y-vectors.
 *** 
 ***  PARAMETERS:
 ***      Type               Name                Description
 ***      ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 ***      const MAP_TraBVx_032_Tab2DS34I2T16651 *map                 data structure
 ***      SInt8              x                   searching for f(X,y)
 ***      UInt16             y                   searching for f(x,Y
 ***      UInt8 *            local_xLow          last index of x-vector
 ***      UInt8 *            local_yLow          last index of y-vector
 ***
 ***  RETURNS:
 ***      SInt16
 ***
 ***  SETTINGS:
 ***      Row axis
 ***      Search algorithm...................: local search
 ***      Boundary points....................: no
 ***      Starting point.....................: - (non-equidistant implementation)
 ***      Scaling unit.......................: - (non-equidistant implementation)
 ***      Column axis
 ***      Search algorithm...................: local search
 ***      Boundary points....................: no
 ***      Starting point.....................: - (non-equidistant implementation)
 ***      Scaling unit.......................: - (non-equidistant implementation)
 ***      Interpolation
 ***      Look-up method.....................: Interpolation - Use end values
 ***      Distances fit into given bitlengths: no
 ***
\**************************************************************************************************/

#define TRABVX_START_SEC_CODE
#include "TraBVx_MemMap.h"
SInt16 TraBVx_032_Tab2DS34I2T16651(const MAP_TraBVx_032_Tab2DS34I2T16651 * map, SInt8 x, UInt16 y,
  UInt8 * local_xLow, UInt8 * local_yLow)
{
   /* SLLocal: Default storage class for local variables | Width: 16 */
   SInt16 Aux_I16;
   SInt16 Aux_I16_a;
   SInt16 Aux_I16_b;
   SInt16 Aux_I16_c;
   UInt16 Aux_U16;
   UInt16 Aux_U16_a;

   /* SLLocal: Default storage class for local variables | Width: 8 */
   SInt8 Aux_I8;
   UInt8 Aux_U8;
   UInt8 Aux_U8_a;

   /* SLLutLocalConst: Default storage class for local variables | Width: 32 */
   const SInt8 * x_table /* Scaling may differ through function reuse. */;
   const UInt16 * y_table /* Scaling may differ through function reuse. */;
   const SInt16 * z_table /* Scaling may differ through function reuse. */;

   x_table = map->x_table;
   y_table = map->y_table;
   z_table = map->z_table;
   Aux_U8 = map->Ny;

   /* Saturation. */
   if (x < *(x_table)) {
      x = x_table[0];
   }
   Aux_I8 = x_table[(UInt8) (map->Nx - 1)];
   if (x > Aux_I8) {
      x = Aux_I8;
   }
   Aux_U8_a = *(local_xLow);
   x_table += Aux_U8_a;

   /* Local search. */
   if (x < *(x_table)) {
      /* Search algorithm for row axis.
         Linear search, start high. */
      do       {
         --(Aux_U8_a);
         --(x_table);
      }
      while (x < *(x_table));
   }
   else {
      /* Search algorithm for row axis. */
      (x_table)++;

      /* Linear search, start low. */
      while (x > *((x_table)++))       {
         (Aux_U8_a)++;
      }
      x_table -= 2 /* 2. */;
   }
   *(local_xLow) = Aux_U8_a;
   z_table += (((UInt16) Aux_U8) * ((UInt16) Aux_U8_a));

   /* Saturation. */
   if (y < *(y_table)) {
      y = y_table[0];
   }
   Aux_U16 = y_table[(UInt8) (Aux_U8 - 1)];
   if (y > Aux_U16) {
      y = Aux_U16;
   }
   Aux_U8_a = *(local_yLow);
   y_table += Aux_U8_a;

   /* Local search. */
   if (y < *(y_table)) {
      /* Search algorithm for column axis.
         Linear search, start high. */
      do       {
         --(Aux_U8_a);
         --(y_table);
      }
      while (y < *(y_table));
   }
   else {
      /* Search algorithm for column axis. */
      (y_table)++;

      /* Linear search, start low. */
      while (y > *((y_table)++))       {
         (Aux_U8_a)++;
      }
      y_table -= 2 /* 2. */;
   }
   *(local_yLow) = Aux_U8_a;
   z_table += Aux_U8_a;

   /* Differences in column axis. */
   Aux_U16 = (UInt16) (y - y_table[0]);
   Aux_U16_a = (UInt16) (y_table[1] - y_table[0]);

   /* Interpolation. */
   if (Aux_U16 == 0) {
      Aux_I16 = z_table[0];
      Aux_I16_a = z_table[Aux_U8];
   }
   else {
      Aux_I16_b = z_table[0];

      /* 1. Y-Interpolation. */
      Aux_I16_c = z_table[1];
      if (Aux_I16_b < Aux_I16_c) {
         /* Positive slope. */
         Aux_I16 = (SInt16) (((UInt16) Aux_I16_b) + ((UInt16) ((((UInt32) (UInt16) (((UInt16)
          Aux_I16_c) - ((UInt16) Aux_I16_b))) * ((UInt32) Aux_U16)) / Aux_U16_a)));
      }
      else {
         /* Negative slope. */
         Aux_I16 = (SInt16) (((UInt16) Aux_I16_b) - ((UInt16) ((((UInt32) (UInt16) (((UInt16)
          Aux_I16_b) - ((UInt16) Aux_I16_c))) * ((UInt32) Aux_U16)) / Aux_U16_a)));
      }
      z_table += Aux_U8;
      Aux_I16_b = z_table[0];

      /* 2. Y-Interpolation. */
      Aux_I16_c = z_table[1];
      if (Aux_I16_b < Aux_I16_c) {
         /* Positive slope. */
         Aux_I16_a = (SInt16) (((UInt16) Aux_I16_b) + ((UInt16) ((((UInt32) (UInt16) (((UInt16)
          Aux_I16_c) - ((UInt16) Aux_I16_b))) * ((UInt32) Aux_U16)) / Aux_U16_a)));
      }
      else {
         /* Negative slope. */
         Aux_I16_a = (SInt16) (((UInt16) Aux_I16_b) - ((UInt16) ((((UInt32) (UInt16) (((UInt16)
          Aux_I16_b) - ((UInt16) Aux_I16_c))) * ((UInt32) Aux_U16)) / Aux_U16_a)));
      }
   }

   /* Differences in row axis. */
   Aux_U8 = (UInt8) (((UInt8) x) - ((UInt8) x_table[0]));
   Aux_U8_a = (UInt8) (((UInt8) x_table[1]) - ((UInt8) x_table[0]));
   if (Aux_U8_a != 0) {
      if (Aux_I16 < Aux_I16_a) {
         /* Positive slope. */
         Aux_I16 += ((SInt16) (UInt16) ((((UInt32) (UInt16) (((UInt16) Aux_I16_a) - ((UInt16)
          Aux_I16))) * ((UInt32) Aux_U8)) / Aux_U8_a));
      }
      else {
         /* Negative slope. */
         Aux_I16 -= ((SInt16) (UInt16) ((((UInt32) (UInt16) (((UInt16) Aux_I16) - ((UInt16)
          Aux_I16_a))) * ((UInt32) Aux_U8)) / Aux_U8_a));
      }
   }
   return Aux_I16;
}

#define TRABVX_STOP_SEC_CODE
#include "TraBVx_MemMap.h"

/**************************************************************************************************\
 ***  FUNCTION:
 ***      TraBVx_032_Tab2DS34I2T16673
 *** 
 ***  DESCRIPTION:
 ***      Generated function for 2D look-up tables with common X- and Y-vectors.
 *** 
 ***  PARAMETERS:
 ***      Type               Name                Description
 ***      ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 ***      const MAP_TraBVx_032_Tab2DS34I2T16673 *map                 data structure
 ***      UInt16             x                   searching for f(X,y)
 ***      SInt16             y                   searching for f(x,Y
 ***      UInt8 *            local_xLow          last index of x-vector
 ***      UInt8 *            local_yLow          last index of y-vector
 ***
 ***  RETURNS:
 ***      SInt16
 ***
 ***  SETTINGS:
 ***      Row axis
 ***      Search algorithm...................: local search
 ***      Boundary points....................: no
 ***      Starting point.....................: - (non-equidistant implementation)
 ***      Scaling unit.......................: - (non-equidistant implementation)
 ***      Column axis
 ***      Search algorithm...................: local search
 ***      Boundary points....................: no
 ***      Starting point.....................: - (non-equidistant implementation)
 ***      Scaling unit.......................: - (non-equidistant implementation)
 ***      Interpolation
 ***      Look-up method.....................: Interpolation - Use end values
 ***      Distances fit into given bitlengths: no
 ***
\**************************************************************************************************/

#define TRABVX_START_SEC_CODE
#include "TraBVx_MemMap.h"
SInt16 TraBVx_032_Tab2DS34I2T16673(const MAP_TraBVx_032_Tab2DS34I2T16673 * map, UInt16 x, SInt16 y,
  UInt8 * local_xLow, UInt8 * local_yLow)
{
   /* SLLocal: Default storage class for local variables | Width: 16 */
   SInt16 Aux_I16;
   SInt16 Aux_I16_a;
   SInt16 Aux_I16_b;
   SInt16 Aux_I16_c;
   UInt16 Aux_U16;
   UInt16 Aux_U16_a;

   /* SLLocal: Default storage class for local variables | Width: 8 */
   UInt8 Aux_U8;
   UInt8 Aux_U8_a;

   /* SLLutLocalConst: Default storage class for local variables | Width: 32 */
   const UInt16 * x_table /* Scaling may differ through function reuse. */;
   const SInt16 * y_table /* Scaling may differ through function reuse. */;
   const SInt16 * z_table /* Scaling may differ through function reuse. */;

   x_table = map->x_table;
   y_table = map->y_table;
   z_table = map->z_table;
   Aux_U8 = map->Ny;

   /* Saturation. */
   if (x < *(x_table)) {
      x = x_table[0];
   }
   Aux_U16 = x_table[(UInt8) (map->Nx - 1)];
   if (x > Aux_U16) {
      x = Aux_U16;
   }
   Aux_U8_a = *(local_xLow);
   x_table += Aux_U8_a;

   /* Local search. */
   if (x < *(x_table)) {
      /* Search algorithm for row axis.
         Linear search, start high. */
      do       {
         --(Aux_U8_a);
         --(x_table);
      }
      while (x < *(x_table));
   }
   else {
      /* Search algorithm for row axis. */
      (x_table)++;

      /* Linear search, start low. */
      while (x > *((x_table)++))       {
         (Aux_U8_a)++;
      }
      x_table -= 2 /* 2. */;
   }
   *(local_xLow) = Aux_U8_a;
   z_table += (((UInt16) Aux_U8) * ((UInt16) Aux_U8_a));

   /* Saturation. */
   if (y < *(y_table)) {
      y = y_table[0];
   }
   Aux_I16 = y_table[(UInt8) (Aux_U8 - 1)];
   if (y > Aux_I16) {
      y = Aux_I16;
   }
   Aux_U8_a = *(local_yLow);
   y_table += Aux_U8_a;

   /* Local search. */
   if (y < *(y_table)) {
      /* Search algorithm for column axis.
         Linear search, start high. */
      do       {
         --(Aux_U8_a);
         --(y_table);
      }
      while (y < *(y_table));
   }
   else {
      /* Search algorithm for column axis. */
      (y_table)++;

      /* Linear search, start low. */
      while (y > *((y_table)++))       {
         (Aux_U8_a)++;
      }
      y_table -= 2 /* 2. */;
   }
   *(local_yLow) = Aux_U8_a;
   z_table += Aux_U8_a;

   /* Differences in column axis. */
   Aux_U16 = (UInt16) (((UInt16) y) - ((UInt16) y_table[0]));
   Aux_U16_a = (UInt16) (((UInt16) y_table[1]) - ((UInt16) y_table[0]));

   /* Interpolation. */
   if (Aux_U16 == 0) {
      Aux_I16 = z_table[0];
      Aux_I16_a = z_table[Aux_U8];
   }
   else {
      Aux_I16_b = z_table[0];

      /* 1. Y-Interpolation. */
      Aux_I16_c = z_table[1];
      if (Aux_I16_b < Aux_I16_c) {
         /* Positive slope. */
         Aux_I16 = (SInt16) (((UInt16) Aux_I16_b) + ((UInt16) ((((UInt32) (UInt16) (((UInt16)
          Aux_I16_c) - ((UInt16) Aux_I16_b))) * ((UInt32) Aux_U16)) / Aux_U16_a)));
      }
      else {
         /* Negative slope. */
         Aux_I16 = (SInt16) (((UInt16) Aux_I16_b) - ((UInt16) ((((UInt32) (UInt16) (((UInt16)
          Aux_I16_b) - ((UInt16) Aux_I16_c))) * ((UInt32) Aux_U16)) / Aux_U16_a)));
      }
      z_table += Aux_U8;
      Aux_I16_b = z_table[0];

      /* 2. Y-Interpolation. */
      Aux_I16_c = z_table[1];
      if (Aux_I16_b < Aux_I16_c) {
         /* Positive slope. */
         Aux_I16_a = (SInt16) (((UInt16) Aux_I16_b) + ((UInt16) ((((UInt32) (UInt16) (((UInt16)
          Aux_I16_c) - ((UInt16) Aux_I16_b))) * ((UInt32) Aux_U16)) / Aux_U16_a)));
      }
      else {
         /* Negative slope. */
         Aux_I16_a = (SInt16) (((UInt16) Aux_I16_b) - ((UInt16) ((((UInt32) (UInt16) (((UInt16)
          Aux_I16_b) - ((UInt16) Aux_I16_c))) * ((UInt32) Aux_U16)) / Aux_U16_a)));
      }
   }

   /* Differences in row axis. */
   Aux_U16 = (UInt16) (x - x_table[0]);
   Aux_U16_a = (UInt16) (x_table[1] - x_table[0]);
   if (Aux_U16_a != 0) {
      if (Aux_I16 < Aux_I16_a) {
         /* Positive slope. */
         Aux_I16 += ((SInt16) (UInt16) ((((UInt32) (UInt16) (((UInt16) Aux_I16_a) - ((UInt16)
          Aux_I16))) * ((UInt32) Aux_U16)) / Aux_U16_a));
      }
      else {
         /* Negative slope. */
         Aux_I16 -= ((SInt16) (UInt16) ((((UInt32) (UInt16) (((UInt16) Aux_I16) - ((UInt16)
          Aux_I16_a))) * ((UInt32) Aux_U16)) / Aux_U16_a));
      }
   }
   return Aux_I16;
}

#define TRABVX_STOP_SEC_CODE
#include "TraBVx_MemMap.h"

/**************************************************************************************************\
 ***  FUNCTION:
 ***      TraBVx_032_Tab2DS34I2T16675
 *** 
 ***  DESCRIPTION:
 ***      Generated function for 2D look-up tables with common X- and Y-vectors.
 *** 
 ***  PARAMETERS:
 ***      Type               Name                Description
 ***      ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 ***      const MAP_TraBVx_032_Tab2DS34I2T16675 *map                 data structure
 ***      SInt8              x                   searching for f(X,y)
 ***      SInt16             y                   searching for f(x,Y
 ***      UInt8 *            local_xLow          last index of x-vector
 ***      UInt8 *            local_yLow          last index of y-vector
 ***
 ***  RETURNS:
 ***      SInt16
 ***
 ***  SETTINGS:
 ***      Row axis
 ***      Search algorithm...................: local search
 ***      Boundary points....................: no
 ***      Starting point.....................: - (non-equidistant implementation)
 ***      Scaling unit.......................: - (non-equidistant implementation)
 ***      Column axis
 ***      Search algorithm...................: local search
 ***      Boundary points....................: no
 ***      Starting point.....................: - (non-equidistant implementation)
 ***      Scaling unit.......................: - (non-equidistant implementation)
 ***      Interpolation
 ***      Look-up method.....................: Interpolation - Use end values
 ***      Distances fit into given bitlengths: no
 ***
\**************************************************************************************************/

#define TRABVX_START_SEC_CODE
#include "TraBVx_MemMap.h"
SInt16 TraBVx_032_Tab2DS34I2T16675(const MAP_TraBVx_032_Tab2DS34I2T16675 * map, SInt8 x, SInt16 y,
  UInt8 * local_xLow, UInt8 * local_yLow)
{
   /* SLLocal: Default storage class for local variables | Width: 16 */
   SInt16 Aux_I16;
   SInt16 Aux_I16_a;
   SInt16 Aux_I16_b;
   SInt16 Aux_I16_c;
   UInt16 Aux_U16;
   UInt16 Aux_U16_a;

   /* SLLocal: Default storage class for local variables | Width: 8 */
   SInt8 Aux_I8;
   UInt8 Aux_U8;
   UInt8 Aux_U8_a;

   /* SLLutLocalConst: Default storage class for local variables | Width: 32 */
   const SInt8 * x_table /* Scaling may differ through function reuse. */;
   const SInt16 * y_table /* Scaling may differ through function reuse. */;
   const SInt16 * z_table /* Scaling may differ through function reuse. */;

   x_table = map->x_table;
   y_table = map->y_table;
   z_table = map->z_table;
   Aux_U8 = map->Ny;

   /* Saturation. */
   if (x < *(x_table)) {
      x = x_table[0];
   }
   Aux_I8 = x_table[(UInt8) (map->Nx - 1)];
   if (x > Aux_I8) {
      x = Aux_I8;
   }
   Aux_U8_a = *(local_xLow);
   x_table += Aux_U8_a;

   /* Local search. */
   if (x < *(x_table)) {
      /* Search algorithm for row axis.
         Linear search, start high. */
      do       {
         --(Aux_U8_a);
         --(x_table);
      }
      while (x < *(x_table));
   }
   else {
      /* Search algorithm for row axis. */
      (x_table)++;

      /* Linear search, start low. */
      while (x > *((x_table)++))       {
         (Aux_U8_a)++;
      }
      x_table -= 2 /* 2. */;
   }
   *(local_xLow) = Aux_U8_a;
   z_table += (((UInt16) Aux_U8) * ((UInt16) Aux_U8_a));

   /* Saturation. */
   if (y < *(y_table)) {
      y = y_table[0];
   }
   Aux_I16 = y_table[(UInt8) (Aux_U8 - 1)];
   if (y > Aux_I16) {
      y = Aux_I16;
   }
   Aux_U8_a = *(local_yLow);
   y_table += Aux_U8_a;

   /* Local search. */
   if (y < *(y_table)) {
      /* Search algorithm for column axis.
         Linear search, start high. */
      do       {
         --(Aux_U8_a);
         --(y_table);
      }
      while (y < *(y_table));
   }
   else {
      /* Search algorithm for column axis. */
      (y_table)++;

      /* Linear search, start low. */
      while (y > *((y_table)++))       {
         (Aux_U8_a)++;
      }
      y_table -= 2 /* 2. */;
   }
   *(local_yLow) = Aux_U8_a;
   z_table += Aux_U8_a;

   /* Differences in column axis. */
   Aux_U16 = (UInt16) (((UInt16) y) - ((UInt16) y_table[0]));
   Aux_U16_a = (UInt16) (((UInt16) y_table[1]) - ((UInt16) y_table[0]));

   /* Interpolation. */
   if (Aux_U16 == 0) {
      Aux_I16 = z_table[0];
      Aux_I16_a = z_table[Aux_U8];
   }
   else {
      Aux_I16_b = z_table[0];

      /* 1. Y-Interpolation. */
      Aux_I16_c = z_table[1];
      if (Aux_I16_b < Aux_I16_c) {
         /* Positive slope. */
         Aux_I16 = (SInt16) (((UInt16) Aux_I16_b) + ((UInt16) ((((UInt32) (UInt16) (((UInt16)
          Aux_I16_c) - ((UInt16) Aux_I16_b))) * ((UInt32) Aux_U16)) / Aux_U16_a)));
      }
      else {
         /* Negative slope. */
         Aux_I16 = (SInt16) (((UInt16) Aux_I16_b) - ((UInt16) ((((UInt32) (UInt16) (((UInt16)
          Aux_I16_b) - ((UInt16) Aux_I16_c))) * ((UInt32) Aux_U16)) / Aux_U16_a)));
      }
      z_table += Aux_U8;
      Aux_I16_b = z_table[0];

      /* 2. Y-Interpolation. */
      Aux_I16_c = z_table[1];
      if (Aux_I16_b < Aux_I16_c) {
         /* Positive slope. */
         Aux_I16_a = (SInt16) (((UInt16) Aux_I16_b) + ((UInt16) ((((UInt32) (UInt16) (((UInt16)
          Aux_I16_c) - ((UInt16) Aux_I16_b))) * ((UInt32) Aux_U16)) / Aux_U16_a)));
      }
      else {
         /* Negative slope. */
         Aux_I16_a = (SInt16) (((UInt16) Aux_I16_b) - ((UInt16) ((((UInt32) (UInt16) (((UInt16)
          Aux_I16_b) - ((UInt16) Aux_I16_c))) * ((UInt32) Aux_U16)) / Aux_U16_a)));
      }
   }

   /* Differences in row axis. */
   Aux_U8 = (UInt8) (((UInt8) x) - ((UInt8) x_table[0]));
   Aux_U8_a = (UInt8) (((UInt8) x_table[1]) - ((UInt8) x_table[0]));
   if (Aux_U8_a != 0) {
      if (Aux_I16 < Aux_I16_a) {
         /* Positive slope. */
         Aux_I16 += ((SInt16) (UInt16) ((((UInt32) (UInt16) (((UInt16) Aux_I16_a) - ((UInt16)
          Aux_I16))) * ((UInt32) Aux_U8)) / Aux_U8_a));
      }
      else {
         /* Negative slope. */
         Aux_I16 -= ((SInt16) (UInt16) ((((UInt32) (UInt16) (((UInt16) Aux_I16) - ((UInt16)
          Aux_I16_a))) * ((UInt32) Aux_U8)) / Aux_U8_a));
      }
   }
   return Aux_I16;
}

#define TRABVX_STOP_SEC_CODE
#include "TraBVx_MemMap.h"

/**************************************************************************************************\
 ***  FUNCTION:
 ***      TraBVx_032_Tab2DS34I2T4169
 *** 
 ***  DESCRIPTION:
 ***      Generated function for 2D look-up tables with common X- and Y-vectors.
 *** 
 ***  PARAMETERS:
 ***      Type               Name                Description
 ***      ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 ***      const MAP_TraBVx_032_Tab2DS34I2T4169 *map                 data structure
 ***      UInt16             x                   searching for f(X,y)
 ***      UInt16             y                   searching for f(x,Y
 ***      UInt8 *            local_xLow          last index of x-vector
 ***      UInt8 *            local_yLow          last index of y-vector
 ***
 ***  RETURNS:
 ***      UInt16
 ***
 ***  SETTINGS:
 ***      Row axis
 ***      Search algorithm...................: local search
 ***      Boundary points....................: no
 ***      Starting point.....................: - (non-equidistant implementation)
 ***      Scaling unit.......................: - (non-equidistant implementation)
 ***      Column axis
 ***      Search algorithm...................: local search
 ***      Boundary points....................: no
 ***      Starting point.....................: - (non-equidistant implementation)
 ***      Scaling unit.......................: - (non-equidistant implementation)
 ***      Interpolation
 ***      Look-up method.....................: Interpolation - Use end values
 ***      Distances fit into given bitlengths: no
 ***
\**************************************************************************************************/

#define TRABVX_START_SEC_CODE
#include "TraBVx_MemMap.h"
UInt16 TraBVx_032_Tab2DS34I2T4169(const MAP_TraBVx_032_Tab2DS34I2T4169 * map, UInt16 x, UInt16 y,
  UInt8 * local_xLow, UInt8 * local_yLow)
{
   /* SLLocal: Default storage class for local variables | Width: 16 */
   UInt16 Aux_U16;
   UInt16 Aux_U16_a;
   UInt16 Aux_U16_b;
   UInt16 Aux_U16_c;
   UInt16 Aux_U16_d;

   /* SLLocal: Default storage class for local variables | Width: 8 */
   UInt8 Aux_U8;
   UInt8 Aux_U8_a;

   /* SLLutLocalConst: Default storage class for local variables | Width: 32 */
   const UInt16 * x_table /* Scaling may differ through function reuse. */;
   const UInt16 * y_table /* Scaling may differ through function reuse. */;
   const UInt16 * z_table /* Scaling may differ through function reuse. */;

   x_table = map->x_table;
   y_table = map->y_table;
   z_table = map->z_table;
   Aux_U8 = map->Ny;

   /* Saturation. */
   if (x < *(x_table)) {
      x = x_table[0];
   }
   Aux_U16 = x_table[(UInt8) (map->Nx - 1)];
   if (x > Aux_U16) {
      x = Aux_U16;
   }
   Aux_U8_a = *(local_xLow);
   x_table += Aux_U8_a;

   /* Local search. */
   if (x < *(x_table)) {
      /* Search algorithm for row axis.
         Linear search, start high. */
      do       {
         --(Aux_U8_a);
         --(x_table);
      }
      while (x < *(x_table));
   }
   else {
      /* Search algorithm for row axis. */
      (x_table)++;

      /* Linear search, start low. */
      while (x > *((x_table)++))       {
         (Aux_U8_a)++;
      }
      x_table -= 2 /* 2. */;
   }
   *(local_xLow) = Aux_U8_a;
   z_table += (((UInt16) Aux_U8) * ((UInt16) Aux_U8_a));

   /* Saturation. */
   if (y < *(y_table)) {
      y = y_table[0];
   }
   Aux_U16 = y_table[(UInt8) (Aux_U8 - 1)];
   if (y > Aux_U16) {
      y = Aux_U16;
   }
   Aux_U8_a = *(local_yLow);
   y_table += Aux_U8_a;

   /* Local search. */
   if (y < *(y_table)) {
      /* Search algorithm for column axis.
         Linear search, start high. */
      do       {
         --(Aux_U8_a);
         --(y_table);
      }
      while (y < *(y_table));
   }
   else {
      /* Search algorithm for column axis. */
      (y_table)++;

      /* Linear search, start low. */
      while (y > *((y_table)++))       {
         (Aux_U8_a)++;
      }
      y_table -= 2 /* 2. */;
   }
   *(local_yLow) = Aux_U8_a;
   z_table += Aux_U8_a;

   /* Differences in column axis. */
   Aux_U16 = (UInt16) (y - y_table[0]);
   Aux_U16_a = (UInt16) (y_table[1] - y);
   Aux_U16_b = (UInt16) (Aux_U16 + Aux_U16_a);

   /* Interpolation. */
   if (Aux_U16_b == 0) {
      Aux_U16_c = z_table[0];
      Aux_U16_d = z_table[Aux_U8];
   }
   else {
      /* 1. Y-Interpolation. */
      Aux_U16_c = (UInt16) ((((UInt32) (((UInt32) Aux_U16_a) * ((UInt32) z_table[0]))) + ((UInt32)
       (((UInt32) Aux_U16) * ((UInt32) z_table[1])))) / Aux_U16_b);
      z_table += Aux_U8;

      /* 2. Y-Interpolation. */
      Aux_U16_d = (UInt16) ((((UInt32) (((UInt32) Aux_U16_a) * ((UInt32) z_table[0]))) + ((UInt32)
       (((UInt32) Aux_U16) * ((UInt32) z_table[1])))) / Aux_U16_b);
   }

   /* Differences in row axis. */
   Aux_U16 = (UInt16) (x - x_table[0]);
   Aux_U16_a = (UInt16) (x_table[1] - x);
   Aux_U16_b = (UInt16) (Aux_U16 + Aux_U16_a);
   if (Aux_U16_b != 0) {
      /* X-Interpolation. */
      Aux_U16_c = (UInt16) ((((UInt32) (((UInt32) Aux_U16_a) * ((UInt32) Aux_U16_c))) + ((UInt32)
       (((UInt32) Aux_U16) * ((UInt32) Aux_U16_d)))) / Aux_U16_b);
   }
   return Aux_U16_c;
}

#define TRABVX_STOP_SEC_CODE
#include "TraBVx_MemMap.h"

/**************************************************************************************************\
 ***  FUNCTION:
 ***      TraBVx_032_Tab2DS34I2T4171
 *** 
 ***  DESCRIPTION:
 ***      Generated function for 2D look-up tables with common X- and Y-vectors.
 *** 
 ***  PARAMETERS:
 ***      Type               Name                Description
 ***      ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 ***      const MAP_TraBVx_032_Tab2DS34I2T4171 *map                 data structure
 ***      SInt8              x                   searching for f(X,y)
 ***      UInt16             y                   searching for f(x,Y
 ***      UInt8 *            local_xLow          last index of x-vector
 ***      UInt8 *            local_yLow          last index of y-vector
 ***
 ***  RETURNS:
 ***      UInt16
 ***
 ***  SETTINGS:
 ***      Row axis
 ***      Search algorithm...................: local search
 ***      Boundary points....................: no
 ***      Starting point.....................: - (non-equidistant implementation)
 ***      Scaling unit.......................: - (non-equidistant implementation)
 ***      Column axis
 ***      Search algorithm...................: local search
 ***      Boundary points....................: no
 ***      Starting point.....................: - (non-equidistant implementation)
 ***      Scaling unit.......................: - (non-equidistant implementation)
 ***      Interpolation
 ***      Look-up method.....................: Interpolation - Use end values
 ***      Distances fit into given bitlengths: no
 ***
\**************************************************************************************************/

#define TRABVX_START_SEC_CODE
#include "TraBVx_MemMap.h"
UInt16 TraBVx_032_Tab2DS34I2T4171(const MAP_TraBVx_032_Tab2DS34I2T4171 * map, SInt8 x, UInt16 y,
  UInt8 * local_xLow, UInt8 * local_yLow)
{
   /* SLLocal: Default storage class for local variables | Width: 16 */
   UInt16 Aux_U16;
   UInt16 Aux_U16_a;
   UInt16 Aux_U16_b;
   UInt16 Aux_U16_c;
   UInt16 Aux_U16_d;

   /* SLLocal: Default storage class for local variables | Width: 8 */
   SInt8 Aux_I8;
   UInt8 Aux_U8;
   UInt8 Aux_U8_a;

   /* SLLutLocalConst: Default storage class for local variables | Width: 32 */
   const SInt8 * x_table /* Scaling may differ through function reuse. */;
   const UInt16 * y_table /* Scaling may differ through function reuse. */;
   const UInt16 * z_table /* Scaling may differ through function reuse. */;

   x_table = map->x_table;
   y_table = map->y_table;
   z_table = map->z_table;
   Aux_U8 = map->Ny;

   /* Saturation. */
   if (x < *(x_table)) {
      x = x_table[0];
   }
   Aux_I8 = x_table[(UInt8) (map->Nx - 1)];
   if (x > Aux_I8) {
      x = Aux_I8;
   }
   Aux_U8_a = *(local_xLow);
   x_table += Aux_U8_a;

   /* Local search. */
   if (x < *(x_table)) {
      /* Search algorithm for row axis.
         Linear search, start high. */
      do       {
         --(Aux_U8_a);
         --(x_table);
      }
      while (x < *(x_table));
   }
   else {
      /* Search algorithm for row axis. */
      (x_table)++;

      /* Linear search, start low. */
      while (x > *((x_table)++))       {
         (Aux_U8_a)++;
      }
      x_table -= 2 /* 2. */;
   }
   *(local_xLow) = Aux_U8_a;
   z_table += (((UInt16) Aux_U8) * ((UInt16) Aux_U8_a));

   /* Saturation. */
   if (y < *(y_table)) {
      y = y_table[0];
   }
   Aux_U16 = y_table[(UInt8) (Aux_U8 - 1)];
   if (y > Aux_U16) {
      y = Aux_U16;
   }
   Aux_U8_a = *(local_yLow);
   y_table += Aux_U8_a;

   /* Local search. */
   if (y < *(y_table)) {
      /* Search algorithm for column axis.
         Linear search, start high. */
      do       {
         --(Aux_U8_a);
         --(y_table);
      }
      while (y < *(y_table));
   }
   else {
      /* Search algorithm for column axis. */
      (y_table)++;

      /* Linear search, start low. */
      while (y > *((y_table)++))       {
         (Aux_U8_a)++;
      }
      y_table -= 2 /* 2. */;
   }
   *(local_yLow) = Aux_U8_a;
   z_table += Aux_U8_a;

   /* Differences in column axis. */
   Aux_U16 = (UInt16) (y - y_table[0]);
   Aux_U16_a = (UInt16) (y_table[1] - y);
   Aux_U16_b = (UInt16) (Aux_U16 + Aux_U16_a);

   /* Interpolation. */
   if (Aux_U16_b == 0) {
      Aux_U16_c = z_table[0];
      Aux_U16_d = z_table[Aux_U8];
   }
   else {
      /* 1. Y-Interpolation. */
      Aux_U16_c = (UInt16) ((((UInt32) (((UInt32) Aux_U16_a) * ((UInt32) z_table[0]))) + ((UInt32)
       (((UInt32) Aux_U16) * ((UInt32) z_table[1])))) / Aux_U16_b);
      z_table += Aux_U8;

      /* 2. Y-Interpolation. */
      Aux_U16_d = (UInt16) ((((UInt32) (((UInt32) Aux_U16_a) * ((UInt32) z_table[0]))) + ((UInt32)
       (((UInt32) Aux_U16) * ((UInt32) z_table[1])))) / Aux_U16_b);
   }

   /* Differences in row axis. */
   Aux_U8 = (UInt8) (((UInt8) x) - ((UInt8) x_table[0]));
   Aux_U8_a = (UInt8) (((UInt8) x_table[1]) - ((UInt8) x_table[0]));
   if (Aux_U8_a != 0) {
      if (Aux_U16_c < Aux_U16_d) {
         /* Positive slope. */
         Aux_U16_c += ((UInt16) ((((UInt32) (UInt16) (Aux_U16_d - Aux_U16_c)) * ((UInt32) Aux_U8))
          / Aux_U8_a));
      }
      else {
         /* Negative slope. */
         Aux_U16_c -= ((UInt16) ((((UInt32) (UInt16) (Aux_U16_c - Aux_U16_d)) * ((UInt32) Aux_U8))
          / Aux_U8_a));
      }
   }
   return Aux_U16_c;
}

#define TRABVX_STOP_SEC_CODE
#include "TraBVx_MemMap.h"

/**************************************************************************************************\
 ***  FUNCTION:
 ***      TraBVx_032_Tab2DS34I2T4193
 *** 
 ***  DESCRIPTION:
 ***      Generated function for 2D look-up tables with common X- and Y-vectors.
 *** 
 ***  PARAMETERS:
 ***      Type               Name                Description
 ***      ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 ***      const MAP_TraBVx_032_Tab2DS34I2T4193 *map                 data structure
 ***      UInt16             x                   searching for f(X,y)
 ***      SInt16             y                   searching for f(x,Y
 ***      UInt8 *            local_xLow          last index of x-vector
 ***      UInt8 *            local_yLow          last index of y-vector
 ***
 ***  RETURNS:
 ***      UInt16
 ***
 ***  SETTINGS:
 ***      Row axis
 ***      Search algorithm...................: local search
 ***      Boundary points....................: no
 ***      Starting point.....................: - (non-equidistant implementation)
 ***      Scaling unit.......................: - (non-equidistant implementation)
 ***      Column axis
 ***      Search algorithm...................: local search
 ***      Boundary points....................: no
 ***      Starting point.....................: - (non-equidistant implementation)
 ***      Scaling unit.......................: - (non-equidistant implementation)
 ***      Interpolation
 ***      Look-up method.....................: Interpolation - Use end values
 ***      Distances fit into given bitlengths: no
 ***
\**************************************************************************************************/

#define TRABVX_START_SEC_CODE
#include "TraBVx_MemMap.h"
UInt16 TraBVx_032_Tab2DS34I2T4193(const MAP_TraBVx_032_Tab2DS34I2T4193 * map, UInt16 x, SInt16 y,
  UInt8 * local_xLow, UInt8 * local_yLow)
{
   /* SLLocal: Default storage class for local variables | Width: 16 */
   SInt16 Aux_I16;
   UInt16 Aux_U16;
   UInt16 Aux_U16_a;
   UInt16 Aux_U16_b;
   UInt16 Aux_U16_c;
   UInt16 Aux_U16_d;
   UInt16 Aux_U16_e;

   /* SLLocal: Default storage class for local variables | Width: 8 */
   UInt8 Aux_U8;
   UInt8 Aux_U8_a;

   /* SLLutLocalConst: Default storage class for local variables | Width: 32 */
   const UInt16 * x_table /* Scaling may differ through function reuse. */;
   const SInt16 * y_table /* Scaling may differ through function reuse. */;
   const UInt16 * z_table /* Scaling may differ through function reuse. */;

   x_table = map->x_table;
   y_table = map->y_table;
   z_table = map->z_table;
   Aux_U8 = map->Ny;

   /* Saturation. */
   if (x < *(x_table)) {
      x = x_table[0];
   }
   Aux_U16 = x_table[(UInt8) (map->Nx - 1)];
   if (x > Aux_U16) {
      x = Aux_U16;
   }
   Aux_U8_a = *(local_xLow);
   x_table += Aux_U8_a;

   /* Local search. */
   if (x < *(x_table)) {
      /* Search algorithm for row axis.
         Linear search, start high. */
      do       {
         --(Aux_U8_a);
         --(x_table);
      }
      while (x < *(x_table));
   }
   else {
      /* Search algorithm for row axis. */
      (x_table)++;

      /* Linear search, start low. */
      while (x > *((x_table)++))       {
         (Aux_U8_a)++;
      }
      x_table -= 2 /* 2. */;
   }
   *(local_xLow) = Aux_U8_a;
   z_table += (((UInt16) Aux_U8) * ((UInt16) Aux_U8_a));

   /* Saturation. */
   if (y < *(y_table)) {
      y = y_table[0];
   }
   Aux_I16 = y_table[(UInt8) (Aux_U8 - 1)];
   if (y > Aux_I16) {
      y = Aux_I16;
   }
   Aux_U8_a = *(local_yLow);
   y_table += Aux_U8_a;

   /* Local search. */
   if (y < *(y_table)) {
      /* Search algorithm for column axis.
         Linear search, start high. */
      do       {
         --(Aux_U8_a);
         --(y_table);
      }
      while (y < *(y_table));
   }
   else {
      /* Search algorithm for column axis. */
      (y_table)++;

      /* Linear search, start low. */
      while (y > *((y_table)++))       {
         (Aux_U8_a)++;
      }
      y_table -= 2 /* 2. */;
   }
   *(local_yLow) = Aux_U8_a;
   z_table += Aux_U8_a;

   /* Differences in column axis. */
   Aux_U16 = (UInt16) (((UInt16) y) - ((UInt16) y_table[0]));
   Aux_U16_a = (UInt16) (((UInt16) y_table[1]) - ((UInt16) y_table[0]));

   /* Interpolation. */
   if (Aux_U16 == 0) {
      Aux_U16_b = z_table[0];
      Aux_U16_c = z_table[Aux_U8];
   }
   else {
      Aux_U16_d = z_table[0];

      /* 1. Y-Interpolation. */
      Aux_U16_e = z_table[1];
      if (Aux_U16_d < Aux_U16_e) {
         /* Positive slope. */
         Aux_U16_b = (UInt16) (Aux_U16_d + ((UInt16) ((((UInt32) (UInt16) (Aux_U16_e - Aux_U16_d)) *
           ((UInt32) Aux_U16)) / Aux_U16_a)));
      }
      else {
         /* Negative slope. */
         Aux_U16_b = (UInt16) (Aux_U16_d - ((UInt16) ((((UInt32) (UInt16) (Aux_U16_d - Aux_U16_e)) *
           ((UInt32) Aux_U16)) / Aux_U16_a)));
      }
      z_table += Aux_U8;
      Aux_U16_d = z_table[0];

      /* 2. Y-Interpolation. */
      Aux_U16_e = z_table[1];
      if (Aux_U16_d < Aux_U16_e) {
         /* Positive slope. */
         Aux_U16_c = (UInt16) (Aux_U16_d + ((UInt16) ((((UInt32) (UInt16) (Aux_U16_e - Aux_U16_d)) *
           ((UInt32) Aux_U16)) / Aux_U16_a)));
      }
      else {
         /* Negative slope. */
         Aux_U16_c = (UInt16) (Aux_U16_d - ((UInt16) ((((UInt32) (UInt16) (Aux_U16_d - Aux_U16_e)) *
           ((UInt32) Aux_U16)) / Aux_U16_a)));
      }
   }

   /* Differences in row axis. */
   Aux_U16 = (UInt16) (x - x_table[0]);
   Aux_U16_a = (UInt16) (x_table[1] - x);
   Aux_U16_d = (UInt16) (Aux_U16 + Aux_U16_a);
   if (Aux_U16_d != 0) {
      /* X-Interpolation. */
      Aux_U16_b = (UInt16) ((((UInt32) (((UInt32) Aux_U16_a) * ((UInt32) Aux_U16_b))) + ((UInt32)
       (((UInt32) Aux_U16) * ((UInt32) Aux_U16_c)))) / Aux_U16_d);
   }
   return Aux_U16_b;
}

#define TRABVX_STOP_SEC_CODE
#include "TraBVx_MemMap.h"

/**************************************************************************************************\
 ***  FUNCTION:
 ***      TraBVx_032_Tab1DS2I2T3
 *** 
 ***  DESCRIPTION:
 ***      Generated function for look-up tables.
 *** 
 ***  PARAMETERS:
 ***      Type               Name                Description
 ***      ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 ***      const MAP_TraBVx_032_Tab1DS2I2T3 *map                 data structure
 ***      SInt8              x                   table input
 ***      UInt8 *            local_xLow          table index found in previous search
 ***
 ***  RETURNS:
 ***      UInt8
 ***
 ***  SETTINGS:
 ***      Search algorithm...................: local search
 ***      Look-up method.....................: Interpolation - Use end values
 ***      Distances fit into given bitlengths: no
 ***      Behavior at boundaries.............: no
 ***      Starting point.....................: - (non-equidistant implementation)
 ***      Scaling unit.......................: - (non-equidistant implementation)
 ***
\**************************************************************************************************/

#define TRABVX_START_SEC_CODE
#include "TraBVx_MemMap.h"
UInt8 TraBVx_032_Tab1DS2I2T3(const MAP_TraBVx_032_Tab1DS2I2T3 * map, SInt8 x, UInt8 * local_xLow)
{
   /* SLLocal: Default storage class for local variables | Width: 8 */
   UInt8 Aux_U8;
   UInt8 Aux_U8_a;
   UInt8 Aux_U8_b;
   UInt8 Aux_U8_c;

   /* SLLutLocalConst: Default storage class for local variables | Width: 32 */
   const SInt8 * x_table /* Scaling may differ through function reuse. */;
   const UInt8 * z_table /* Scaling may differ through function reuse. */;

   x_table = map->x_table;
   z_table = map->z_table;
   if (x <= *(x_table)) {
      *(local_xLow) = 0;

      /* Saturation. */
      return z_table[0];
   }
   if (x >= x_table[(UInt8) (map->Nx - 1)]) {
      *(local_xLow) = (UInt8) (map->Nx - 2);
      return z_table[(UInt8) (map->Nx - 1)];
   }
   Aux_U8 = *(local_xLow);
   x_table += Aux_U8;

   /* Local search. */
   if (x < *(x_table)) {
      /* Linear search, start high. */
      do       {
         --(Aux_U8);
         --(x_table);
      }
      while (x < *(x_table));
   }
   else {
      /* Linear search, start low. */
      (x_table)++;
      while (x > *((x_table)++))       {
         (Aux_U8)++;
      }
      x_table -= 2 /* 2. */;
   }
   *(local_xLow) = Aux_U8;
   z_table += Aux_U8;
   Aux_U8 = *((z_table)++);
   Aux_U8_a = *(z_table);

   /* Interpolation. */
   Aux_U8_b = (UInt8) (((UInt8) x) - ((UInt8) x_table[0]));
   Aux_U8_c = (UInt8) (((UInt8) x_table[1]) - ((UInt8) x_table[0]));
   if (Aux_U8 <= Aux_U8_a) {
      /* Positive slope. */
      Aux_U8 += ((UInt8) ((((UInt16) (UInt8) (Aux_U8_a - Aux_U8)) * ((UInt16) Aux_U8_b)) / 
       Aux_U8_c));
   }
   else {
      /* Negative slope. */
      Aux_U8 -= ((UInt8) ((((UInt16) (UInt8) (Aux_U8 - Aux_U8_a)) * ((UInt16) Aux_U8_b)) / 
       Aux_U8_c));
   }
   return Aux_U8;
}

#define TRABVX_STOP_SEC_CODE
#include "TraBVx_MemMap.h"

/**************************************************************************************************\
 ***  FUNCTION:
 ***      TraBVx_032_Tab1DS2I4T0
 *** 
 ***  DESCRIPTION:
 ***      Generated function for look-up tables.
 *** 
 ***  PARAMETERS:
 ***      Type               Name                Description
 ***      ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 ***      const MAP_TraBVx_032_Tab1DS2I4T0 *map                 data structure
 ***      UInt8              x                   table input
 ***      UInt8 *            local_xLow          table index found in previous search
 ***
 ***  RETURNS:
 ***      UInt8
 ***
 ***  SETTINGS:
 ***      Search algorithm...................: local search
 ***      Look-up method.....................: Use input nearest
 ***      Distances fit into given bitlengths: - (no interpolation)
 ***      Behavior at boundaries.............: no
 ***      Starting point.....................: - (non-equidistant implementation)
 ***      Scaling unit.......................: - (non-equidistant implementation)
 ***
\**************************************************************************************************/

#define TRABVX_START_SEC_CODE
#include "TraBVx_MemMap.h"
UInt8 TraBVx_032_Tab1DS2I4T0(const MAP_TraBVx_032_Tab1DS2I4T0 * map, UInt8 x, UInt8 * local_xLow)
{
   /* SLLocal: Default storage class for local variables | Width: 8 */
   UInt8 Aux_U8;

   /* SLLutLocalConst: Default storage class for local variables | Width: 32 */
   const UInt8 * x_table /* Scaling may differ through function reuse. */;
   const UInt8 * z_table /* Scaling may differ through function reuse. */;

   x_table = map->x_table;
   z_table = map->z_table;
   if (x <= *(x_table)) {
      *(local_xLow) = 0;

      /* Saturation. */
      return z_table[0];
   }
   if (x >= x_table[(UInt8) (map->Nx - 1)]) {
      *(local_xLow) = (UInt8) (map->Nx - 2);
      return z_table[(UInt8) (map->Nx - 1)];
   }
   Aux_U8 = *(local_xLow);
   x_table += Aux_U8;

   /* Local search. */
   if (x < *(x_table)) {
      /* Linear search, start high. */
      do       {
         --(Aux_U8);
         --(x_table);
      }
      while (x < *(x_table));
   }
   else {
      /* Linear search, start low. */
      (x_table)++;
      while (x > *((x_table)++))       {
         (Aux_U8)++;
      }
      x_table -= 2 /* 2. */;
   }
   *(local_xLow) = Aux_U8;
   z_table += Aux_U8;

   /* Look-up method: Use Input Nearest */
   return ((((UInt8) (x - x_table[0])) < ((UInt8) (x_table[1] - x))) ? z_table[0] : z_table[1]);
}

#define TRABVX_STOP_SEC_CODE
#include "TraBVx_MemMap.h"

/*----------------------------------------------------------------------------*\
  MODULE LOCAL FUNCTION DEFINITIONS
\*----------------------------------------------------------------------------*/

#endif/*_TRABVX_032_LUT_C_ */
/*----------------------------------------------------------------------------*\
  END OF FILE
\*----------------------------------------------------------------------------*/
